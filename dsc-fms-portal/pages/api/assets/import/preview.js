// POST /api/assets/import/preview
// Alias for /api/assets/import/validate — same multipart contract, same response shape.
// Asset Master v2 Phase 2 — exposed under "preview" semantics for UI consumption.

export { config, default } from './validate';
